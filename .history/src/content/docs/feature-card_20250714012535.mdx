---
title: "feature-card"
description: "A stylish feature-card component for modern UIs, built with accessibility and animation in mind."
---

<ComponentPreview componentName="featureCard" />

## Installation

<Tabs defaultValue="cli">

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">
  <Cli />
</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install dependencies</Step>

<Cli
  dependencies={[
    {
      label: "npm",
      command: "npm i tw-animate-css",
    },
    {
      label: "pnpm",
      command: "pnpm add tw-animate-css",
    },
    {
      label: "bun",
      command: "bun add tw-animate-css",
    },
    {
      label: "yarn",
      command: "yarn add tw-animate-css",
    },
  ]}
/>

<Step>Add the required CSS animations</Step>

Add the following animations to your `globals.css` or others file.

<CodeBlock
  isLanguage={false} code={`
@import "tailwindcss";
@import "tw-animate-css";

@keyframes float {
0% {
transform: translateY(0px);
}
50% {
transform: translateY(-10px);
}
100% {
transform: translateY(0px);
}
}
`}
/>

<Step>Copy and paste the following code into your project.</Step>

`components/nurui/feature-card.tsx`

<CodeBlock componentName="featureCard" fileName="feature-card" />

<Step>Update the import paths to match your project setup.</Step>
   
</Steps>

</TabsContent>

</Tabs>

| Prop              | Type                                                               | Default                               | Description                                                |
| ----------------- | ------------------------------------------------------------------ | ------------------------------------- | ---------------------------------------------------------- |
| `children`        | `ReactNode`                                                        | **Required**                          | Elements or content inside the bordered box.               |
| `className`       | `string`                                                           | `''`                                  | Additional custom classes to apply to the outer div.       |
| `animationMode`   | `'auto-rotate'` \| `'rotate-on-hover'` \| `'stop-rotate-on-hover'` | `'auto-rotate'`                       | Defines when the gradient border rotates.                  |
| `animationSpeed`  | `number`                                                           | `5`                                   | Speed of the rotation in seconds. Lower is faster.         |
| `gradientColors`  | `{ primary: string; secondary: string; accent: string }`           | `{ '#584827', '#c7a03c', '#f9de90' }` | Defines the gradient color stops for the rotating border.  |
| `backgroundColor` | `string`                                                           | `'#1b1d2b'`                           | Background color of the inner box.                         |
| `borderWidth`     | `number`                                                           | `2`                                   | Width of the gradient border in pixels.                    |
| `borderRadius`    | `number`                                                           | `20`                                  | Border radius of the box in pixels.                        |
| `style`           | `React.CSSProperties`                                              | `{}`                                  | Inline styles to further customize the component.          |
| `...props`        | `HTMLAttributes<HTMLDivElement>` (except `className`)              | —                                     | Other native div props like `id`, `title`, `onClick`, etc. |
